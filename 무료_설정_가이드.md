# 🆓 완전 무료 설정 가이드

## ✅ 준비 완료!

코드가 Cloudinary와 Neon을 사용하도록 수정되었습니다.

---

## 📝 1단계: Neon 데이터베이스 (무료)

### 가입 및 생성

1. **https://neon.tech** 접속
2. **"Sign up"** 클릭 → GitHub 로그인
3. **"Create a project"** 클릭
4. 정보 입력:
   ```
   Project name: vibebox
   Region: AWS Asia Pacific (Singapore)
   ```
5. **"Create project"** 클릭

### Connection String 복사

프로젝트 생성 후 화면에 표시되는 **Connection string** 복사:

```
postgresql://[username]:[password]@[host]/[database]?sslmode=require
```

예시:
```
postgresql://vibebox_owner:abc123xyz@ep-cool-sound-12345.ap-southeast-1.aws.neon.tech/vibebox?sslmode=require
```

---

## 📝 2단계: Cloudinary 이미지 호스팅 (무료 25GB)

### 가입

1. **https://cloudinary.com** 접속
2. **"Sign Up for Free"** 클릭
3. 이메일, 비밀번호 입력
4. 이메일 인증

### API 정보 복사

1. **Dashboard** 접속
2. **Account Details** 섹션에서 복사:
   - **Cloud name**: 예) `dxyz123abc`
   - **API Key**: 예) `123456789012345`
   - **API Secret**: 예) `abcdefghijklmnopqrstuvwxyz`

---

## 📝 3단계: Netlify 환경 변수 설정

### 접속

1. **https://app.netlify.com** 접속
2. **1vibebox** 사이트 클릭
3. **Site configuration** → **Environment variables**

### 변수 추가 (총 7개)

**"Add a variable"** 버튼을 클릭하고 하나씩 추가:

#### 1. 데이터베이스
```
Key: DATABASE_URL
Value: [Neon에서 복사한 Connection string]
Scopes: All scopes
```

#### 2. JWT Secret
```
Key: JWT_SECRET
Value: my-super-secret-jwt-key-for-vibebox-production-2024
Scopes: All scopes
```

#### 3-5. Cloudinary
```
Key: CLOUDINARY_CLOUD_NAME
Value: [Cloudinary Cloud name]
Scopes: All scopes

Key: CLOUDINARY_API_KEY
Value: [Cloudinary API Key]
Scopes: All scopes

Key: CLOUDINARY_API_SECRET
Value: [Cloudinary API Secret]
Scopes: All scopes
```

#### 6-7. 기타
```
Key: NEXT_PUBLIC_BASE_URL
Value: https://1vibebox.netlify.app
Scopes: All scopes

Key: DEV_LOGIN
Value: false
Scopes: All scopes
```

---

## 📝 4단계: 데이터베이스 초기화 (로컬에서)

### .env.local 파일 수정

프로젝트 폴더의 `.env.local` 파일을 열고 수정:

```env
# Neon 데이터베이스 URL
DATABASE_URL="postgresql://[복사한 Neon URL]"

# Cloudinary
CLOUDINARY_CLOUD_NAME="[복사한 Cloud name]"
CLOUDINARY_API_KEY="[복사한 API Key]"
CLOUDINARY_API_SECRET="[복사한 API Secret]"

# JWT
JWT_SECRET="my-super-secret-jwt-key-for-vibebox-production-2024"

# Public URL
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Admin
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="strong-initial-password-123"

# DEV 모드 비활성화
DEV_LOGIN=false
DEV_EVAL=false
```

### Prisma 마이그레이션 실행

터미널에서 실행:

```bash
# Prisma 클라이언트 생성
npx prisma generate

# 데이터베이스 마이그레이션
npx prisma migrate deploy

# 관리자 계정 생성
npx prisma db seed
```

---

## 📝 5단계: Git 푸시 및 배포

```bash
git add .
git commit -m "Add Cloudinary support for free file hosting"
git push
```

Netlify가 자동으로 재배포합니다 (2-3분 소요).

---

## 📝 6단계: 테스트

### 배포 완료 확인

1. **https://app.netlify.com** → 1vibebox
2. **Deploys** 탭
3. 최신 배포 상태: **Published** (초록색)

### 로그인 테스트

1. **https://1vibebox.netlify.app/login** 접속
2. 로그인:
   ```
   이메일: admin@example.com
   비밀번호: strong-initial-password-123
   ```
3. 로그인 성공 → `/admin` 페이지로 이동

### 기능 테스트

- [ ] 새 콘텐츠 작성
- [ ] 이미지 업로드
- [ ] 배경 이미지 변경
- [ ] 사이트 설정 변경

---

## ✅ 완료!

이제 완전히 무료로 모든 기능을 사용할 수 있습니다:

- ✅ 데이터베이스: Neon (무료 3GB)
- ✅ 이미지 호스팅: Cloudinary (무료 25GB)
- ✅ 웹 호스팅: Netlify (무료)

**총 비용: $0/월**

---

## 🐛 문제 해결

### Prisma 마이그레이션 실패

```bash
# Prisma 클라이언트 재생성
npx prisma generate

# 다시 시도
npx prisma migrate deploy
```

### 로그인 실패

1. Netlify 환경 변수 확인:
   - `DATABASE_URL` 정확한지
   - `JWT_SECRET` 설정되었는지
   - `DEV_LOGIN=false`인지

2. 재배포:
   - Deploys → Trigger deploy → Deploy site

### 이미지 업로드 실패

1. Cloudinary 환경 변수 확인:
   - `CLOUDINARY_CLOUD_NAME`
   - `CLOUDINARY_API_KEY`
   - `CLOUDINARY_API_SECRET`

2. Cloudinary Dashboard에서 API 정보 재확인

---

## 📞 도움이 필요하면

어느 단계에서 막혔는지 알려주세요:
1. Neon 가입/설정
2. Cloudinary 가입/설정
3. Netlify 환경 변수
4. Prisma 마이그레이션
5. 배포/테스트
